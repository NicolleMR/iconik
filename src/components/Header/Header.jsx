import "./header.scss";

const Header = () => (
  <header className="header">
    <div className="header__user">Nicolle Montilla R.</div>
    <button className="header__button" type="button">
      Salir
    </button>
  </header>
);

export default Header;
